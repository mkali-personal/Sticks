to connect to the device, you can use the following command:

```bash
python -m mpremote connect COM8
```

where `COM8` is the port where the device is connected. You can find the port in the device manager.